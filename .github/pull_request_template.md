## Summary

- 

## Why

- 

## Validation

- [ ] Backend tests pass (`pytest -q`)
- [ ] Backend lint passes (`ruff check .`)
- [ ] Frontend lint passes (`npm run lint`)
- [ ] TypeScript passes (`npm run typecheck`)
- [ ] Frontend production build passes (`npm run build`)

## Privacy / data impact

Describe any change to location handling, logs, external requests, credentials, or persistence. Write `None` if there is no impact.
